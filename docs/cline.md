# cline config

Moved — see **[CLINE_CONFIG.md](CLINE_CONFIG.md)** (canonical; kept in sync with the
`t2-containerized-agent-workspace` repo). Covers the base URL per container runtime
(podman t2-devbox → `host.containers.internal`, docker `lario-dev-*` → `ollama`), the
`providers.json` example, and which local models make sense as an agent brain.

For paid/cloud agents (Claude Code, Codex, …) see **[AGENTS.md](AGENTS.md)**.
