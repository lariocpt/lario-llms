# attic — one-off scripts and session notes, kept for reference (2026-07-17)

Retired during the CachyOS cleanup: one-shot fixers (Bifrost DB pokers, Windows
config patchers), model download/USB-export helpers, vision test harnesses, and
planning/session docs. Most contain stale `/mnt/Shared` or old-home paths and
will not run as-is. Nothing here is part of the live stack.
